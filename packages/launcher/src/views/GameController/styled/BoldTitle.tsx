import { styled, Typography } from '@mui/material';

export const BoldTitle: typeof Typography = styled(Typography)`
    font-weight: bold;
    white-space: nowrap;
    font-family: 'Noto Sans SC', system-ui, -apple-system, 'Open Sans', 'Helvetica Neue', sans-serif;
` as typeof Typography;
