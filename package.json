{
  "name": "seerh5-assistant-project",
  "private": true,
  "type": "module",
  "version": "0.4.0",
  "license": "MPL-2.0",
  "workspaces": [
    "./packages/core",
    "./packages/app"
  ],
  "scripts": {
    "start": "npm run dev:app",
    "dev:core": "npm run dev --workspace=sa-core",
    "dev:app": "npm run dev --workspace=seerh5-assistant-app",
    "build:core": "npm run build --workspace=sa-core",
    "build:app": "npm run build --workspace=seerh5-assistant-app"
  },
  "devDependencies": {
    "typescript": "^5.0.4",
    "eslint": "^8.41.0",
    "rimraf": "^5.0.1",
    "http-proxy-middleware": "^3.0.0-beta.0"
  }
}