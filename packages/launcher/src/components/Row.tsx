import { Stack, styled } from '@mui/material';

export const Row = styled(Stack)`
    flex-direction: row;
    align-items: center;
    justify-content: flex-start;
    width: 100%;
` as typeof Stack;
